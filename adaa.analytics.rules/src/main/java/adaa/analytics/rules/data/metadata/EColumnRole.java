package adaa.analytics.rules.data.metadata;

public enum EColumnRole {
    regular,
    confidence,
    attribute,
    contrast_attribute,
    id,
    label,
    prediction,
    cluster,
    weight,
    cost,
    batch,
    outlier,
    survival_time,
    classification_cost,
    base_value,
    not_defined;
}
