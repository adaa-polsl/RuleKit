package adaa.analytics.rules.data.metadata;

public enum EColumnRole {
    regular,
    confidence,
    attribute,
    id,
    label,
    prediction,
    cluster,
    weight,
    batch,
    outlier,
    survival_time,
    classification_cost,
    base_value,
    not_defined;
}
